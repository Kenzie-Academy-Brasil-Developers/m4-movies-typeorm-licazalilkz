import {
  CreateMovie,
  ReadMovie,
  RepositoryMovie,
  UpdateMovie,
} from "./movie.interface";
import { pagination, paginationParams } from "./pagination.interface";

export {
  CreateMovie,
  ReadMovie,
  RepositoryMovie,
  UpdateMovie,
  pagination,
  paginationParams,
};
